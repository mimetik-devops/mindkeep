"""What the installers start.

Briefcase runs the module named after the app — `mindkeep-app` is `mindkeep_app` — and
`python -m mindkeep` is the CLI, so this is a shim over the tray app in mindkeep.app.
"""
