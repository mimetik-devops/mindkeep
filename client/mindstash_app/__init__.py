"""What the installers start.

Briefcase runs the module named after the app — `mindstash-app` is `mindstash_app` — and
`python -m mindstash` is the CLI, so this is a shim over the tray app in mindstash.app.
"""
